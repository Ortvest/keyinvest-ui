import { Fragment } from 'react';

import { SendPassword } from '@modules/Auth/SendPassword';

export const SendPasswordReset = (): JSX.Element => {
  return (
    <Fragment>
      <SendPassword />
    </Fragment>
  );
};
