import { Fragment } from 'react';

import { SentPassword } from '@modules/SentPassword';

export const SentPasswordReset = () => {
  return (
    <Fragment>
      <SentPassword />
    </Fragment>
  );
};
