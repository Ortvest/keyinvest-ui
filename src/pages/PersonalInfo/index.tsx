import { Fragment } from 'react/jsx-runtime';

import { PersInformation } from '@modules/PersInformation';

export const PersonalInfo = (): JSX.Element => {
  return (
    <Fragment>
      <PersInformation />
    </Fragment>
  );
};
