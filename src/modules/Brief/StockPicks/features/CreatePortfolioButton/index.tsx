import React from 'react';

import '../../styles/styles.css'
import classNames from 'classnames';

export const CreatePortfolioButton = (): React.ReactNode => {
  return (
    <button className={classNames('brief-button')}>Create analytics</button>
  )
}