import { PersonalForm } from '@modules/PersInformation/layout/PersonalInfo';

export const PersInformation = (): JSX.Element => {
  return (
    <section>
      <PersonalForm />
    </section>
  );
};
